import { setupWorker } from 'msw';
//import { getBookingEvents } from './routes/bookings';

// Setup worker with all the handlers to be used with browser
const worker = ''
//const worker = setupWorker(...[getBookingEvents]);
export default worker;